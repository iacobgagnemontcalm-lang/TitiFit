# Refuse l'installation sur une version de Node non supportée plutôt que de
# laisser échouer Expo plus tard avec une erreur incompréhensible
# (sur Node 18 : "parseEnv is not a function").
engine-strict=true
