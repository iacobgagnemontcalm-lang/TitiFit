import { ComingSoon } from '@/components/ui';

export default function AddTab() {
  return (
    <ComingSoon
      title="Ajouter un résultat"
      phase="Phase 2 · 3"
      description="Choisis un test, entre ta performance, et l'app recalcule percentile, rating, catégorie, Overall, XP, PR et achievements."
      icon="add-circle-outline"
    />
  );
}
